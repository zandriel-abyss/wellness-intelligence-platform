# Copy this file to .env and adjust if needed:
#   cp .env.example .env
#
# For local demo with Docker Postgres, these defaults work as-is.

NODE_ENV=development
PORT=3001
FRONTEND_URL=http://localhost:3000

# Database (must match docker-compose.yml if you use Docker)
DATABASE_URL=postgresql://wellness_user:wellness_password@localhost:5432/wellness_db

# Auth (use a long random string in production)
JWT_SECRET=change-this-in-development

# Optional
REDIS_URL=redis://localhost:6379

# Mistral AI — required for real AI analysis. Without a valid key, the app uses mock results.
# Get a key: https://console.mistral.ai/
MISTRAL_API_KEY=sk-test-placeholder-key-for-development
MISTRAL_MODEL=mistral-large-latest
